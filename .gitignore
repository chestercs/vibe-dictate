/target
Cargo.lock
*.log

# Upstream VibeVoice checkout — not our code, user clones it into the repo root.
# See README "Backend setup" for how to populate this folder.
/VibeVoice/

# Per-host overrides for docker compose
.env
