/target
Cargo.lock
*.log

# Generated at build time by build.rs when absent. Drop your own icon.ico
# here to override the default blue-square-with-white-dot branding.
/assets/icon.ico

# Upstream VibeVoice checkout — populated by vibevoice-lab/setup_lab.sh for the
# experimental stack. Not needed for the core OpenAI-compat STT backend.
/vibevoice-lab/VibeVoice/

# Per-host overrides for docker compose
.env
